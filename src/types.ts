interface ICar {
    id: number,
    name: string,
    color: string
}

interface IEngine {
    velocity: number,
    distance: number
}

interface IWinner {
    id: number,
    wins: number,
    time: number
}