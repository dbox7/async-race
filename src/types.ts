export interface ICar {
    id: number,
    name: string,
    color: string
}

export interface IEngine {
    velocity: number,
    distance: number
}

export interface IWinner {
    id: number,
    wins: number,
    time: number
}
